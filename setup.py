"""Setup.py for backward compatibility. Use pyproject.toml instead."""

from setuptools import setup

# Setup configuration is in pyproject.toml
# This file exists for backward compatibility with tools that expect it
setup()
